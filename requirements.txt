# Runtime dependencies
# The core tool uses only the Python standard library.
# Install these extras for development and testing.

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Linting & formatting (optional, for contributors)
ruff>=0.4.0
